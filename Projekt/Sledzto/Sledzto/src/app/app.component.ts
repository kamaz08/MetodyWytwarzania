import { Component } from '@angular/core';

@Component({
  selector: 'my-app',
  template: `<mat-card>
    <websitelist></websitelist>
</mat-card>`,
})
export class AppComponent  { name = 'Angular'; }
